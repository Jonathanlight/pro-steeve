<div class="row properties">
    <div class="col-xs-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Identité</h3>
            </div>
            <div class="panel-body">
                <strong>ID :</strong> {% if user.id %}{{ user.id }}{% endif %}<br/>
                <strong>Nom :</strong> {% if user.firstName %}{{ user.firstName }}{% endif %}<br/>
                <strong>Prénom :</strong> {% if user.lastName %}{{ user.lastName }}{% endif %}<br/>
                <strong>Adresse e-mail :</strong> {% if user.email %}{{ user.email }}{% endif %}<br/>
            </div>
        </div>
    </div>
</div>