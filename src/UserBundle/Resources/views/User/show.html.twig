{% extends "@User/user.base.html.twig" %}

{% block body %}
    {% if app.user %}
        <div class="btn-group" role="group" style="margin-bottom: 20px;">
            <a id="edit-commercial" class="btn btn-default" href="{{ path('userEdit', {id: user.id}) }}"><i class="fa fa-pencil"></i>&nbsp;&nbsp;Modifier</a>
        {% if is_granted('ROLE_SUPER_ADMIN') %}<a id="del-commercial" class="btn btn-danger" href="{{ path('userConfirmDelete', {id: user.id}) }}"><i class="fa fa-trash"></i>&nbsp;&nbsp;Supprimer</a>{% endif %}
        </div>
    {% endif %}

    {{ include('@User/User/detail-content.html.twig', {user: user}) }}

    <div><a class="btn-default btn abutton" href="#" onclick="goBack();"><span class="glyphicon glyphicon-chevron-left"></span>Retour</a></div>
{% endblock %}