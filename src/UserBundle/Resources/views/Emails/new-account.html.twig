{% extends 'Emails/base.html.twig' %}
{% block body %}
    <div class="mail-content" style="padding-top: 10px">
        <h1>Votre compte STEEVE est disponible</h1>
        <div class="row">
            <div class="col-xs-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title">{{ user.firstName }} {{ user.lastName }}</h3>
                    </div>
                    <div class="panel-body">
                        <strong>Identifiant :&nbsp;</strong>{{ user.username }}<br/>
                        <strong>Mot de passe :&nbsp;</strong>{{ password }}<br/>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}